import styled from "@emotion/styled";

const StyledRecipeGrid = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 8px;

  padding: 8px;
`;

export default StyledRecipeGrid;
