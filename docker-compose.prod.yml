version: "3.6"
services:
  graphql-engine:
    depends_on:
      - "postgres"
      - "webhook"
    environment:
      ACTION_BASE_ENDPOINT: http://webhook:80/api
  webhook:
    image: nickwangtech/tnt-webhook
    restart: always
    ports:
      - "5001:80"

volumes:
  db_data:
